# Legacy table: workflow_stage_definitions (seeded every boot; read by nothing today).
# phase_name / prerequisites / conditions added by migration m20260101_000005_wsd_flow_columns.
# Constraint (not structurally expressible here): UNIQUE (workflow_id, stage_code) = uq_workflow_stage_code.
# Legacy index ix_stage_def_order is on sequence_order.
- name: WorkflowStageDefinition
  id: 7b1e4a20-0002-4a10-9c02-a0b1c2d3e4f1
  is_table: true
  facets: [audited]
  props:

  - name: id
    fragment: property-primary-key-uuid

  - name: workflow_id
    caption: Workflow
    data_type: Uuid
    is_required: true
    foreign_key:
      type_name: WorkflowDefinition

  - name: stage_name
    caption: Stage Name
    fragment: property-string-caption

  - name: stage_code
    fragment: property-string-required

  - name: sequence_order
    fragment: property-int-md
    is_required: true

  - name: description
    fragment: property-string

  - name: phase_name
    fragment: property-string-required

  - name: assigned_roles
    fragment: property-json

  - name: prerequisites
    fragment: property-json

  - name: conditions
    fragment: property-json

  - name: parallel_execution
    fragment: property-boolean-toggle
    default_value: false

  - name: auto_advance
    fragment: property-boolean-toggle
    default_value: false

  - name: sla_days
    fragment: property-int-md

  - name: checklist_template
    fragment: property-json
