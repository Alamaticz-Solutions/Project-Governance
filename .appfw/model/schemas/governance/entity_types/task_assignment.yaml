# Legacy table: task_assignments (fully unwired — schema fidelity only).
# Constraint (not structurally expressible here): UNIQUE (task_id, assignee_id) = uq_task_assignee.
# assignee_id and assigned_by_id both FK -> User (Set A / spec 001); referenced here only.
- name: TaskAssignment
  id: 7b1e4a20-0006-4a10-9c06-a0b1c2d3e4f5
  is_table: true
  facets: [audited]
  props:

  - name: id
    fragment: property-primary-key-uuid

  - name: task_id
    caption: Task
    data_type: Uuid
    is_required: true
    foreign_key:
      type_name: WorkflowTask

  - name: assignee_id
    caption: Assignee
    data_type: Uuid
    is_required: true
    foreign_key:
      type_name: User

  - name: assigned_at
    fragment: property-date-time

  - name: assigned_by_id
    caption: Assigned By
    data_type: Uuid
    foreign_key:
      type_name: User

  - name: accepted_at
    fragment: property-date-time

  - name: completed_at
    fragment: property-date-time

  - name: notes
    fragment: property-string
