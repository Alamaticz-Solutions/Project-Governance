--
--    Generated by app_gen.
--    by app_gen/_templates/database/postgresql/create_tables_sql/_mod.j2
--    Defines storage tables for entity_types.
--

DO $$
BEGIN






    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'attachments')
    THEN
        RAISE NOTICE 'ALTERING table attachments';

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "file_name" varchar;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "file_type" varchar;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "file_size" integer;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "s3_key" varchar;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "s3_url" varchar;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "upload_status" varchar;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "uploaded_by_id" uuid;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "ai_extracted" boolean;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "ai_extraction_data" jsonb;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "uploaded_at" timestamptz;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.attachments ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table attachments';
        CREATE TABLE governance.attachments
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"file_name" varchar
            ,"file_type" varchar
            ,"file_size" integer
            ,"s3_key" varchar
            ,"s3_url" varchar
            ,"upload_status" varchar
            ,"uploaded_by_id" uuid
            ,"ai_extracted" boolean
            ,"ai_extraction_data" jsonb
            ,"uploaded_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'audit_events')
    THEN
        RAISE NOTICE 'ALTERING table audit_events';

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "entity_type" varchar;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "entity_id" varchar;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "action" varchar;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "old_values" jsonb;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "new_values" jsonb;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "performed_by_id" uuid;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "ip_address" varchar;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "user_agent" varchar;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "performed_at" timestamptz;

        ALTER TABLE governance.audit_events ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table audit_events';
        CREATE TABLE governance.audit_events
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"entity_type" varchar
            ,"entity_id" varchar
            ,"action" varchar
            ,"old_values" jsonb
            ,"new_values" jsonb
            ,"performed_by_id" uuid
            ,"ip_address" varchar
            ,"user_agent" varchar
            ,"performed_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'checklist_items')
    THEN
        RAISE NOTICE 'ALTERING table checklist_items';

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "task_id" uuid;

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "item_text" varchar;

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "is_completed" boolean;

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "completed_by_id" uuid;

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "completed_at" timestamptz;

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "sequence_order" integer;

        ALTER TABLE governance.checklist_items ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table checklist_items';
        CREATE TABLE governance.checklist_items
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"task_id" uuid
            ,"item_text" varchar
            ,"is_completed" boolean
            ,"completed_by_id" uuid
            ,"completed_at" timestamptz
            ,"sequence_order" integer
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'comments')
    THEN
        RAISE NOTICE 'ALTERING table comments';

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "task_id" uuid;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "gate_review_id" uuid;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "author_id" uuid;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "content" varchar;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "parent_id" uuid;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "is_internal" boolean;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.comments ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table comments';
        CREATE TABLE governance.comments
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"task_id" uuid
            ,"gate_review_id" uuid
            ,"author_id" uuid
            ,"content" varchar
            ,"parent_id" uuid
            ,"is_internal" boolean
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'email_queue_items')
    THEN
        RAISE NOTICE 'ALTERING table email_queue_items';

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "to_email" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "to_name" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "subject" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "template_name" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "template_data" jsonb;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "html_body" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "text_body" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "attempts" integer;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "max_attempts" integer;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "error_message" varchar;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "scheduled_at" timestamptz;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "sent_at" timestamptz;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.email_queue_items ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table email_queue_items';
        CREATE TABLE governance.email_queue_items
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"to_email" varchar
            ,"to_name" varchar
            ,"subject" varchar
            ,"template_name" varchar
            ,"template_data" jsonb
            ,"html_body" varchar
            ,"text_body" varchar
            ,"status" varchar
            ,"attempts" integer
            ,"max_attempts" integer
            ,"error_message" varchar
            ,"scheduled_at" timestamptz
            ,"sent_at" timestamptz
            ,"created_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'gate_reviews')
    THEN
        RAISE NOTICE 'ALTERING table gate_reviews';

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "gate_code" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "gate_name" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "committee" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "assigned_role" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "decision" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "decision_by_id" uuid;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "decision_at" timestamptz;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "decision_notes" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "checklist_items" jsonb;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "submitted_at" timestamptz;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "due_date" timestamptz;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "priority" varchar;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.gate_reviews ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table gate_reviews';
        CREATE TABLE governance.gate_reviews
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"gate_code" varchar
            ,"gate_name" varchar
            ,"committee" varchar
            ,"assigned_role" varchar
            ,"status" varchar
            ,"decision" varchar
            ,"decision_by_id" uuid
            ,"decision_at" timestamptz
            ,"decision_notes" varchar
            ,"checklist_items" jsonb
            ,"submitted_at" timestamptz
            ,"due_date" timestamptz
            ,"priority" varchar
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'gate_submissions')
    THEN
        RAISE NOTICE 'ALTERING table gate_submissions';

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "stage" varchar;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "decision" varchar;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "data" jsonb;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "submitted_by" uuid;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "submitted_at" timestamptz;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.gate_submissions ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table gate_submissions';
        CREATE TABLE governance.gate_submissions
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"stage" varchar
            ,"status" varchar
            ,"decision" varchar
            ,"data" jsonb
            ,"submitted_by" uuid
            ,"submitted_at" timestamptz
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'graph_subscriptions')
    THEN
        RAISE NOTICE 'ALTERING table graph_subscriptions';

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "subscription_id" varchar;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "resource" varchar;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "notification_url" varchar;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "client_state" varchar;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "expiration_date_time" timestamptz;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.graph_subscriptions ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table graph_subscriptions';
        CREATE TABLE governance.graph_subscriptions
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"subscription_id" varchar
            ,"resource" varchar
            ,"notification_url" varchar
            ,"client_state" varchar
            ,"expiration_date_time" timestamptz
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'knowledge_chunks')
    THEN
        RAISE NOTICE 'ALTERING table knowledge_chunks';

        ALTER TABLE governance.knowledge_chunks ADD COLUMN IF NOT EXISTS "document_id" uuid;

        ALTER TABLE governance.knowledge_chunks ADD COLUMN IF NOT EXISTS "chunk_text" varchar;

        ALTER TABLE governance.knowledge_chunks ADD COLUMN IF NOT EXISTS "metadata" jsonb;

        ALTER TABLE governance.knowledge_chunks ADD COLUMN IF NOT EXISTS "sequence_order" integer;

        ALTER TABLE governance.knowledge_chunks ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table knowledge_chunks';
        CREATE TABLE governance.knowledge_chunks
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"document_id" uuid
            ,"chunk_text" varchar
            ,"metadata" jsonb
            ,"sequence_order" integer
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'knowledge_documents')
    THEN
        RAISE NOTICE 'ALTERING table knowledge_documents';

        ALTER TABLE governance.knowledge_documents ADD COLUMN IF NOT EXISTS "title" varchar;

        ALTER TABLE governance.knowledge_documents ADD COLUMN IF NOT EXISTS "document_type" varchar;

        ALTER TABLE governance.knowledge_documents ADD COLUMN IF NOT EXISTS "source_url" varchar;

        ALTER TABLE governance.knowledge_documents ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.knowledge_documents ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table knowledge_documents';
        CREATE TABLE governance.knowledge_documents
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"title" varchar
            ,"document_type" varchar
            ,"source_url" varchar
            ,"created_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'meetings')
    THEN
        RAISE NOTICE 'ALTERING table meetings';

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "subject" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "source" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "start_time" timestamptz;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "end_time" timestamptz;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "organizer_email" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "graph_online_meeting_id" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "graph_organizer_id" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "graph_organizer_user_id" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "graph_transcript_id" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "graph_event_id" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "join_url" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "external_ref" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "transcript_vtt" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "transcript_text" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "summary" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "decisions" jsonb[];

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "action_items" jsonb[];

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "agenda_items" jsonb[];

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "attendees" jsonb[];

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "contains_process_flow" boolean;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "process_name" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "bpmn_xml" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "bpmn_status" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "error_message" varchar;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.meetings ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table meetings';
        CREATE TABLE governance.meetings
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"subject" varchar
            ,"source" varchar
            ,"status" varchar
            ,"start_time" timestamptz
            ,"end_time" timestamptz
            ,"organizer_email" varchar
            ,"graph_online_meeting_id" varchar
            ,"graph_organizer_id" varchar
            ,"graph_organizer_user_id" varchar
            ,"graph_transcript_id" varchar
            ,"graph_event_id" varchar
            ,"join_url" varchar
            ,"external_ref" varchar
            ,"transcript_vtt" varchar
            ,"transcript_text" varchar
            ,"summary" varchar
            ,"decisions" jsonb[]
            ,"action_items" jsonb[]
            ,"agenda_items" jsonb[]
            ,"attendees" jsonb[]
            ,"contains_process_flow" boolean
            ,"process_name" varchar
            ,"bpmn_xml" varchar
            ,"bpmn_status" varchar
            ,"error_message" varchar
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'notifications')
    THEN
        RAISE NOTICE 'ALTERING table notifications';

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "recipient_id" uuid;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "notification_type" varchar;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "title" varchar;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "message" varchar;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "action_url" varchar;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "is_read" boolean;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "read_at" timestamptz;

        ALTER TABLE governance.notifications ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table notifications';
        CREATE TABLE governance.notifications
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"recipient_id" uuid
            ,"project_id" uuid
            ,"notification_type" varchar
            ,"title" varchar
            ,"message" varchar
            ,"action_url" varchar
            ,"is_read" boolean
            ,"created_at" timestamptz
            ,"read_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'projects')
    THEN
        RAISE NOTICE 'ALTERING table projects';

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "project_number" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "project_name" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "business_unit" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "department" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "manager_id" uuid;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "sponsor_name" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "sponsor_email" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "description" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "problem_statement" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "business_value" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "strategic_alignment" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "requestor_name" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "request_type" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "desired_outcome" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "what_do_you_do_today" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "what_transpires_if_nothing" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "notes" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "budget_estimated" double precision;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "budget_approved" double precision;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "budget_type" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "requested_start_date" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "requested_end_date" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "actual_start_date" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "actual_end_date" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "priority" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "risk_level" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "it_involvement" boolean;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "vendor_required" boolean;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "has_phi_data" boolean;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "is_clinical" boolean;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "is_hipaa_applicable" boolean;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "smartsheet_row_id" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "smartsheet_sheet_url" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "jira_ticket_id" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "duplicate_of_id" uuid;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "is_duplicate" boolean;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "ai_extracted_data" jsonb;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "current_stage" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "current_status" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "current_owner_role" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "last_stage_completed" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "workflow_status" varchar;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "submitted_at" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "archived_at" timestamptz;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.projects ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table projects';
        CREATE TABLE governance.projects
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_number" varchar
            ,"project_name" varchar
            ,"business_unit" varchar
            ,"department" varchar
            ,"manager_id" uuid
            ,"sponsor_name" varchar
            ,"sponsor_email" varchar
            ,"description" varchar
            ,"problem_statement" varchar
            ,"business_value" varchar
            ,"strategic_alignment" varchar
            ,"requestor_name" varchar
            ,"request_type" varchar
            ,"desired_outcome" varchar
            ,"what_do_you_do_today" varchar
            ,"what_transpires_if_nothing" varchar
            ,"notes" varchar
            ,"budget_estimated" double precision
            ,"budget_approved" double precision
            ,"budget_type" varchar
            ,"requested_start_date" timestamptz
            ,"requested_end_date" timestamptz
            ,"actual_start_date" timestamptz
            ,"actual_end_date" timestamptz
            ,"priority" varchar
            ,"risk_level" varchar
            ,"status" varchar
            ,"it_involvement" boolean
            ,"vendor_required" boolean
            ,"has_phi_data" boolean
            ,"is_clinical" boolean
            ,"is_hipaa_applicable" boolean
            ,"smartsheet_row_id" varchar
            ,"smartsheet_sheet_url" varchar
            ,"jira_ticket_id" varchar
            ,"duplicate_of_id" uuid
            ,"is_duplicate" boolean
            ,"ai_extracted_data" jsonb
            ,"current_stage" varchar
            ,"current_status" varchar
            ,"current_owner_role" varchar
            ,"last_stage_completed" varchar
            ,"workflow_status" varchar
            ,"submitted_at" timestamptz
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"archived_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'project_approvals')
    THEN
        RAISE NOTICE 'ALTERING table project_approvals';

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "approval_stage" varchar;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "assigned_role" varchar;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "assigned_user_id" uuid;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "decision" varchar;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "comments" varchar;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "approved_by" uuid;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "approved_at" timestamptz;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "sequence_order" integer;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "notification_sent" boolean;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.project_approvals ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table project_approvals';
        CREATE TABLE governance.project_approvals
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"approval_stage" varchar
            ,"assigned_role" varchar
            ,"assigned_user_id" uuid
            ,"status" varchar
            ,"decision" varchar
            ,"comments" varchar
            ,"approved_by" uuid
            ,"approved_at" timestamptz
            ,"sequence_order" integer
            ,"notification_sent" boolean
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'project_fields')
    THEN
        RAISE NOTICE 'ALTERING table project_fields';

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "field_name" varchar;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "field_value" jsonb;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "is_ai_suggested" boolean;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "ai_confidence" double precision;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "ai_source_document" varchar;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "ai_extracted_text" varchar;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "updated_by_id" uuid;

        ALTER TABLE governance.project_fields ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table project_fields';
        CREATE TABLE governance.project_fields
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"field_name" varchar
            ,"field_value" jsonb
            ,"is_ai_suggested" boolean
            ,"ai_confidence" double precision
            ,"ai_source_document" varchar
            ,"ai_extracted_text" varchar
            ,"updated_at" timestamptz
            ,"updated_by_id" uuid
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'project_stakeholders')
    THEN
        RAISE NOTICE 'ALTERING table project_stakeholders';

        ALTER TABLE governance.project_stakeholders ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.project_stakeholders ADD COLUMN IF NOT EXISTS "user_id" uuid;

        ALTER TABLE governance.project_stakeholders ADD COLUMN IF NOT EXISTS "role" varchar;

        ALTER TABLE governance.project_stakeholders ADD COLUMN IF NOT EXISTS "added_at" timestamptz;

        ALTER TABLE governance.project_stakeholders ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table project_stakeholders';
        CREATE TABLE governance.project_stakeholders
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"user_id" uuid
            ,"role" varchar
            ,"added_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'risk_items')
    THEN
        RAISE NOTICE 'ALTERING table risk_items';

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "risk_title" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "risk_description" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "risk_category" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "severity" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "probability" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "impact" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "mitigation_plan" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "owner_id" uuid;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "identified_at" timestamptz;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "resolved_at" timestamptz;

        ALTER TABLE governance.risk_items ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table risk_items';
        CREATE TABLE governance.risk_items
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"risk_title" varchar
            ,"risk_description" varchar
            ,"risk_category" varchar
            ,"severity" varchar
            ,"probability" varchar
            ,"impact" varchar
            ,"mitigation_plan" varchar
            ,"owner_id" uuid
            ,"status" varchar
            ,"identified_at" timestamptz
            ,"resolved_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'task_assignments')
    THEN
        RAISE NOTICE 'ALTERING table task_assignments';

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "task_id" uuid;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "assignee_id" uuid;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "assigned_at" timestamptz;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "assigned_by_id" uuid;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "accepted_at" timestamptz;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "completed_at" timestamptz;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "notes" varchar;

        ALTER TABLE governance.task_assignments ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table task_assignments';
        CREATE TABLE governance.task_assignments
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"task_id" uuid
            ,"assignee_id" uuid
            ,"assigned_at" timestamptz
            ,"assigned_by_id" uuid
            ,"accepted_at" timestamptz
            ,"completed_at" timestamptz
            ,"notes" varchar
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'users')
    THEN
        RAISE NOTICE 'ALTERING table users';

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "email" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "username" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "full_name" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "hashed_password" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "role" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "department" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "job_title" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "phone" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "avatar_url" varchar;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "is_active" boolean;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "is_verified" boolean;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "last_login" timestamptz;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "updated_at" timestamptz;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.users ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table users';
        CREATE TABLE governance.users
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"email" varchar
            ,"username" varchar
            ,"full_name" varchar
            ,"hashed_password" varchar
            ,"role" varchar
            ,"department" varchar
            ,"job_title" varchar
            ,"phone" varchar
            ,"avatar_url" varchar
            ,"is_active" boolean
            ,"is_verified" boolean
            ,"last_login" timestamptz
            ,"created_at" timestamptz
            ,"updated_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'workflow_definitions')
    THEN
        RAISE NOTICE 'ALTERING table workflow_definitions';

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "name" varchar;

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "description" varchar;

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "definition_version" integer;

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "is_active" boolean;

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "created_at" timestamptz;

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.workflow_definitions ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table workflow_definitions';
        CREATE TABLE governance.workflow_definitions
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"name" varchar
            ,"description" varchar
            ,"definition_version" integer
            ,"is_active" boolean
            ,"created_at" timestamptz
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'workflow_instances')
    THEN
        RAISE NOTICE 'ALTERING table workflow_instances';

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "project_id" uuid;

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "definition_id" uuid;

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "current_stage_id" uuid;

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "started_at" timestamptz;

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "completed_at" timestamptz;

        ALTER TABLE governance.workflow_instances ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table workflow_instances';
        CREATE TABLE governance.workflow_instances
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"project_id" uuid
            ,"definition_id" uuid
            ,"current_stage_id" uuid
            ,"status" varchar
            ,"started_at" timestamptz
            ,"completed_at" timestamptz
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'workflow_stages')
    THEN
        RAISE NOTICE 'ALTERING table workflow_stages';

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "workflow_instance_id" uuid;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "stage_definition_id" uuid;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "stage_name" varchar;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "stage_code" varchar;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "sequence_order" integer;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "started_at" timestamptz;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "completed_at" timestamptz;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "due_date" timestamptz;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "notes" varchar;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "version" bigint NOT NULL DEFAULT 0;

        ALTER TABLE governance.workflow_stages ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table workflow_stages';
        CREATE TABLE governance.workflow_stages
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"workflow_instance_id" uuid
            ,"stage_definition_id" uuid
            ,"stage_name" varchar
            ,"stage_code" varchar
            ,"sequence_order" integer
            ,"status" varchar
            ,"started_at" timestamptz
            ,"completed_at" timestamptz
            ,"due_date" timestamptz
            ,"notes" varchar
            ,"version" bigint NOT NULL DEFAULT 0
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'workflow_stage_definitions')
    THEN
        RAISE NOTICE 'ALTERING table workflow_stage_definitions';

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "workflow_id" uuid;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "stage_name" varchar;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "stage_code" varchar;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "sequence_order" integer;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "description" varchar;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "phase_name" varchar;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "assigned_roles" jsonb;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "prerequisites" jsonb;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "conditions" jsonb;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "parallel_execution" boolean;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "auto_advance" boolean;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "sla_days" integer;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "checklist_template" jsonb;

        ALTER TABLE governance.workflow_stage_definitions ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table workflow_stage_definitions';
        CREATE TABLE governance.workflow_stage_definitions
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"workflow_id" uuid
            ,"stage_name" varchar
            ,"stage_code" varchar
            ,"sequence_order" integer
            ,"description" varchar
            ,"phase_name" varchar
            ,"assigned_roles" jsonb
            ,"prerequisites" jsonb
            ,"conditions" jsonb
            ,"parallel_execution" boolean
            ,"auto_advance" boolean
            ,"sla_days" integer
            ,"checklist_template" jsonb
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;

    IF EXISTS(SELECT TRUE FROM pg_type where typnamespace = (select oid from pg_catalog.pg_namespace where nspname = 'governance') AND typname = 'workflow_tasks')
    THEN
        RAISE NOTICE 'ALTERING table workflow_tasks';

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "stage_id" uuid;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "task_name" varchar;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "task_description" varchar;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "task_type" varchar;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "assigned_role" varchar;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "status" varchar;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "is_required" boolean;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "sequence_order" integer;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "due_date" timestamptz;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "completed_at" timestamptz;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "notes" varchar;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "metadata" jsonb;

        ALTER TABLE governance.workflow_tasks ADD COLUMN IF NOT EXISTS "record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''));

    ELSE
        RAISE NOTICE 'CREATING, table workflow_tasks';
        CREATE TABLE governance.workflow_tasks
        (
            "id" uuid NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY
            ,"stage_id" uuid
            ,"task_name" varchar
            ,"task_description" varchar
            ,"task_type" varchar
            ,"assigned_role" varchar
            ,"status" varchar
            ,"is_required" boolean
            ,"sequence_order" integer
            ,"due_date" timestamptz
            ,"completed_at" timestamptz
            ,"notes" varchar
            ,"metadata" jsonb
            ,"record_locator" varchar NOT NULL DEFAULT ('rl_' || replace(gen_random_uuid()::text, '-', ''))
        );
    END IF;




    CREATE TABLE IF NOT EXISTS governance.attachments_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.attachments_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.attachments_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'Attachment';

    ALTER TABLE governance.attachments_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'attachments';

    ALTER TABLE governance.attachments_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'attachments_audit';


    CREATE INDEX IF NOT EXISTS idx_attachments_audit_record
        ON governance.attachments_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_attachments_audit_actor
        ON governance.attachments_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_attachments_audit_chain
        ON governance.attachments_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_attachments_audit_event_hash
        ON governance.attachments_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.checklist_items_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.checklist_items_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.checklist_items_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'ChecklistItem';

    ALTER TABLE governance.checklist_items_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'checklist_items';

    ALTER TABLE governance.checklist_items_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'checklist_items_audit';


    CREATE INDEX IF NOT EXISTS idx_checklist_items_audit_record
        ON governance.checklist_items_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_checklist_items_audit_actor
        ON governance.checklist_items_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_checklist_items_audit_chain
        ON governance.checklist_items_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_checklist_items_audit_event_hash
        ON governance.checklist_items_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.comments_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.comments_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.comments_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'Comment';

    ALTER TABLE governance.comments_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'comments';

    ALTER TABLE governance.comments_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'comments_audit';


    CREATE INDEX IF NOT EXISTS idx_comments_audit_record
        ON governance.comments_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_comments_audit_actor
        ON governance.comments_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_comments_audit_chain
        ON governance.comments_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_comments_audit_event_hash
        ON governance.comments_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.gate_reviews_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.gate_reviews_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.gate_reviews_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'GateReview';

    ALTER TABLE governance.gate_reviews_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'gate_reviews';

    ALTER TABLE governance.gate_reviews_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'gate_reviews_audit';


    CREATE INDEX IF NOT EXISTS idx_gate_reviews_audit_record
        ON governance.gate_reviews_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_gate_reviews_audit_actor
        ON governance.gate_reviews_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_gate_reviews_audit_chain
        ON governance.gate_reviews_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_gate_reviews_audit_event_hash
        ON governance.gate_reviews_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.gate_submissions_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.gate_submissions_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.gate_submissions_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'GateSubmission';

    ALTER TABLE governance.gate_submissions_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'gate_submissions';

    ALTER TABLE governance.gate_submissions_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'gate_submissions_audit';


    CREATE INDEX IF NOT EXISTS idx_gate_submissions_audit_record
        ON governance.gate_submissions_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_gate_submissions_audit_actor
        ON governance.gate_submissions_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_gate_submissions_audit_chain
        ON governance.gate_submissions_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_gate_submissions_audit_event_hash
        ON governance.gate_submissions_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.meetings_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.meetings_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.meetings_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'Meeting';

    ALTER TABLE governance.meetings_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'meetings';

    ALTER TABLE governance.meetings_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'meetings_audit';


    CREATE INDEX IF NOT EXISTS idx_meetings_audit_record
        ON governance.meetings_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_meetings_audit_actor
        ON governance.meetings_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_meetings_audit_chain
        ON governance.meetings_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_meetings_audit_event_hash
        ON governance.meetings_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.projects_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.projects_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.projects_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'Project';

    ALTER TABLE governance.projects_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'projects';

    ALTER TABLE governance.projects_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'projects_audit';


    CREATE INDEX IF NOT EXISTS idx_projects_audit_record
        ON governance.projects_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_projects_audit_actor
        ON governance.projects_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_projects_audit_chain
        ON governance.projects_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_projects_audit_event_hash
        ON governance.projects_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.project_approvals_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.project_approvals_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.project_approvals_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'ProjectApproval';

    ALTER TABLE governance.project_approvals_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'project_approvals';

    ALTER TABLE governance.project_approvals_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'project_approvals_audit';


    CREATE INDEX IF NOT EXISTS idx_project_approvals_audit_record
        ON governance.project_approvals_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_project_approvals_audit_actor
        ON governance.project_approvals_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_project_approvals_audit_chain
        ON governance.project_approvals_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_project_approvals_audit_event_hash
        ON governance.project_approvals_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.project_fields_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.project_fields_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.project_fields_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'ProjectField';

    ALTER TABLE governance.project_fields_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'project_fields';

    ALTER TABLE governance.project_fields_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'project_fields_audit';


    CREATE INDEX IF NOT EXISTS idx_project_fields_audit_record
        ON governance.project_fields_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_project_fields_audit_actor
        ON governance.project_fields_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_project_fields_audit_chain
        ON governance.project_fields_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_project_fields_audit_event_hash
        ON governance.project_fields_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.project_stakeholders_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.project_stakeholders_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.project_stakeholders_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'ProjectStakeholder';

    ALTER TABLE governance.project_stakeholders_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'project_stakeholders';

    ALTER TABLE governance.project_stakeholders_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'project_stakeholders_audit';


    CREATE INDEX IF NOT EXISTS idx_project_stakeholders_audit_record
        ON governance.project_stakeholders_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_project_stakeholders_audit_actor
        ON governance.project_stakeholders_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_project_stakeholders_audit_chain
        ON governance.project_stakeholders_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_project_stakeholders_audit_event_hash
        ON governance.project_stakeholders_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.risk_items_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.risk_items_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.risk_items_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'RiskItem';

    ALTER TABLE governance.risk_items_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'risk_items';

    ALTER TABLE governance.risk_items_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'risk_items_audit';


    CREATE INDEX IF NOT EXISTS idx_risk_items_audit_record
        ON governance.risk_items_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_risk_items_audit_actor
        ON governance.risk_items_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_risk_items_audit_chain
        ON governance.risk_items_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_risk_items_audit_event_hash
        ON governance.risk_items_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.task_assignments_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.task_assignments_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.task_assignments_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'TaskAssignment';

    ALTER TABLE governance.task_assignments_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'task_assignments';

    ALTER TABLE governance.task_assignments_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'task_assignments_audit';


    CREATE INDEX IF NOT EXISTS idx_task_assignments_audit_record
        ON governance.task_assignments_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_task_assignments_audit_actor
        ON governance.task_assignments_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_task_assignments_audit_chain
        ON governance.task_assignments_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_task_assignments_audit_event_hash
        ON governance.task_assignments_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.users_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.users_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.users_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'User';

    ALTER TABLE governance.users_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'users';

    ALTER TABLE governance.users_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'users_audit';


    CREATE INDEX IF NOT EXISTS idx_users_audit_record
        ON governance.users_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_users_audit_actor
        ON governance.users_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_users_audit_chain
        ON governance.users_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_users_audit_event_hash
        ON governance.users_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.workflow_instances_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.workflow_instances_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.workflow_instances_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'WorkflowInstance';

    ALTER TABLE governance.workflow_instances_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'workflow_instances';

    ALTER TABLE governance.workflow_instances_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'workflow_instances_audit';


    CREATE INDEX IF NOT EXISTS idx_workflow_instances_audit_record
        ON governance.workflow_instances_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_instances_audit_actor
        ON governance.workflow_instances_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_instances_audit_chain
        ON governance.workflow_instances_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_instances_audit_event_hash
        ON governance.workflow_instances_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.workflow_stages_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.workflow_stages_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.workflow_stages_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'WorkflowStage';

    ALTER TABLE governance.workflow_stages_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'workflow_stages';

    ALTER TABLE governance.workflow_stages_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'workflow_stages_audit';


    CREATE INDEX IF NOT EXISTS idx_workflow_stages_audit_record
        ON governance.workflow_stages_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_stages_audit_actor
        ON governance.workflow_stages_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_stages_audit_chain
        ON governance.workflow_stages_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_stages_audit_event_hash
        ON governance.workflow_stages_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.workflow_stage_definitions_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.workflow_stage_definitions_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.workflow_stage_definitions_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'WorkflowStageDefinition';

    ALTER TABLE governance.workflow_stage_definitions_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'workflow_stage_definitions';

    ALTER TABLE governance.workflow_stage_definitions_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'workflow_stage_definitions_audit';


    CREATE INDEX IF NOT EXISTS idx_workflow_stage_definitions_audit_record
        ON governance.workflow_stage_definitions_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_stage_definitions_audit_actor
        ON governance.workflow_stage_definitions_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_stage_definitions_audit_chain
        ON governance.workflow_stage_definitions_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_stage_definitions_audit_event_hash
        ON governance.workflow_stage_definitions_audit ("event_hash");


    CREATE TABLE IF NOT EXISTS governance.workflow_tasks_audit
    (
        audit_id varchar NOT NULL PRIMARY KEY
        ,occurred_at timestamptz NOT NULL
        ,tenant_id varchar
        ,actor_user_name varchar NOT NULL
        ,actor_roles jsonb NOT NULL
        ,action varchar NOT NULL
        ,outcome varchar NOT NULL
        ,schema_name varchar NOT NULL
        ,entity_name varchar NOT NULL
        ,table_name varchar NOT NULL
        ,audit_table_name varchar NOT NULL
        ,record_id varchar
        ,before_json jsonb NULL
        ,after_json jsonb NULL
        ,diff_json jsonb NOT NULL
        ,policy_json jsonb NULL
        ,redactions_json jsonb NOT NULL
        ,chain_scope varchar NOT NULL
        ,prev_hash varchar
        ,event_hash varchar NOT NULL
        ,signature varchar NULL
    );


    ALTER TABLE governance.workflow_tasks_audit ADD COLUMN IF NOT EXISTS schema_name varchar NOT NULL DEFAULT 'governance';

    ALTER TABLE governance.workflow_tasks_audit ADD COLUMN IF NOT EXISTS entity_name varchar NOT NULL DEFAULT 'WorkflowTask';

    ALTER TABLE governance.workflow_tasks_audit ADD COLUMN IF NOT EXISTS table_name varchar NOT NULL DEFAULT 'workflow_tasks';

    ALTER TABLE governance.workflow_tasks_audit ADD COLUMN IF NOT EXISTS audit_table_name varchar NOT NULL DEFAULT 'workflow_tasks_audit';


    CREATE INDEX IF NOT EXISTS idx_workflow_tasks_audit_record
        ON governance.workflow_tasks_audit ("record_id", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_tasks_audit_actor
        ON governance.workflow_tasks_audit ("actor_user_name", "occurred_at");

    CREATE INDEX IF NOT EXISTS idx_workflow_tasks_audit_chain
        ON governance.workflow_tasks_audit ("chain_scope", "occurred_at");

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_tasks_audit_event_hash
        ON governance.workflow_tasks_audit ("event_hash");





    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'attachments'
        AND indexname = 'ux_attachments_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_attachments_record_locator';
        CREATE UNIQUE INDEX ux_attachments_record_locator
        ON governance.attachments ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_attachments_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'attachments'
        AND indexname = 'idx_attachments_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_attachments_project_id';
        CREATE INDEX idx_attachments_project_id
        ON governance.attachments ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_attachments_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'attachments'
        AND indexname = 'idx_attachments_uploaded_by_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_attachments_uploaded_by_id';
        CREATE INDEX idx_attachments_uploaded_by_id
        ON governance.attachments ("uploaded_by_id");
    ELSE
        RAISE NOTICE 'Index idx_attachments_uploaded_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'audit_events'
        AND indexname = 'ux_audit_events_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_audit_events_record_locator';
        CREATE UNIQUE INDEX ux_audit_events_record_locator
        ON governance.audit_events ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_audit_events_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'audit_events'
        AND indexname = 'idx_audit_events_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_audit_events_project_id';
        CREATE INDEX idx_audit_events_project_id
        ON governance.audit_events ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_audit_events_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'audit_events'
        AND indexname = 'idx_audit_events_performed_by_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_audit_events_performed_by_id';
        CREATE INDEX idx_audit_events_performed_by_id
        ON governance.audit_events ("performed_by_id");
    ELSE
        RAISE NOTICE 'Index idx_audit_events_performed_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'checklist_items'
        AND indexname = 'ux_checklist_items_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_checklist_items_record_locator';
        CREATE UNIQUE INDEX ux_checklist_items_record_locator
        ON governance.checklist_items ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_checklist_items_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'checklist_items'
        AND indexname = 'idx_checklist_items_task_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_checklist_items_task_id';
        CREATE INDEX idx_checklist_items_task_id
        ON governance.checklist_items ("task_id");
    ELSE
        RAISE NOTICE 'Index idx_checklist_items_task_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'checklist_items'
        AND indexname = 'idx_checklist_items_completed_by_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_checklist_items_completed_by_id';
        CREATE INDEX idx_checklist_items_completed_by_id
        ON governance.checklist_items ("completed_by_id");
    ELSE
        RAISE NOTICE 'Index idx_checklist_items_completed_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'comments'
        AND indexname = 'ux_comments_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_comments_record_locator';
        CREATE UNIQUE INDEX ux_comments_record_locator
        ON governance.comments ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_comments_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'comments'
        AND indexname = 'idx_comments_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_comments_project_id';
        CREATE INDEX idx_comments_project_id
        ON governance.comments ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_comments_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'comments'
        AND indexname = 'idx_comments_task_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_comments_task_id';
        CREATE INDEX idx_comments_task_id
        ON governance.comments ("task_id");
    ELSE
        RAISE NOTICE 'Index idx_comments_task_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'comments'
        AND indexname = 'idx_comments_gate_review_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_comments_gate_review_id';
        CREATE INDEX idx_comments_gate_review_id
        ON governance.comments ("gate_review_id");
    ELSE
        RAISE NOTICE 'Index idx_comments_gate_review_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'comments'
        AND indexname = 'idx_comments_author_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_comments_author_id';
        CREATE INDEX idx_comments_author_id
        ON governance.comments ("author_id");
    ELSE
        RAISE NOTICE 'Index idx_comments_author_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'comments'
        AND indexname = 'idx_comments_parent_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_comments_parent_id';
        CREATE INDEX idx_comments_parent_id
        ON governance.comments ("parent_id");
    ELSE
        RAISE NOTICE 'Index idx_comments_parent_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'email_queue_items'
        AND indexname = 'ux_email_queue_items_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_email_queue_items_record_locator';
        CREATE UNIQUE INDEX ux_email_queue_items_record_locator
        ON governance.email_queue_items ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_email_queue_items_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_reviews'
        AND indexname = 'ux_gate_reviews_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_gate_reviews_record_locator';
        CREATE UNIQUE INDEX ux_gate_reviews_record_locator
        ON governance.gate_reviews ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_gate_reviews_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_reviews'
        AND indexname = 'idx_gate_reviews_status'
    ) THEN
        RAISE NOTICE 'Creating index idx_gate_reviews_status';
        CREATE INDEX idx_gate_reviews_status
        ON governance.gate_reviews ("status");
    ELSE
        RAISE NOTICE 'Index idx_gate_reviews_status already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_reviews'
        AND indexname = 'idx_gate_reviews_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_gate_reviews_project_id';
        CREATE INDEX idx_gate_reviews_project_id
        ON governance.gate_reviews ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_gate_reviews_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_reviews'
        AND indexname = 'idx_gate_reviews_decision_by_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_gate_reviews_decision_by_id';
        CREATE INDEX idx_gate_reviews_decision_by_id
        ON governance.gate_reviews ("decision_by_id");
    ELSE
        RAISE NOTICE 'Index idx_gate_reviews_decision_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_submissions'
        AND indexname = 'ux_gate_submissions_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_gate_submissions_record_locator';
        CREATE UNIQUE INDEX ux_gate_submissions_record_locator
        ON governance.gate_submissions ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_gate_submissions_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_submissions'
        AND indexname = 'idx_gate_submissions_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_gate_submissions_project_id';
        CREATE INDEX idx_gate_submissions_project_id
        ON governance.gate_submissions ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_gate_submissions_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_submissions'
        AND indexname = 'idx_gate_submissions_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_gate_submissions_project_id';
        CREATE INDEX idx_gate_submissions_project_id
        ON governance.gate_submissions ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_gate_submissions_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'gate_submissions'
        AND indexname = 'idx_gate_submissions_submitted_by'
    ) THEN
        RAISE NOTICE 'Creating index idx_gate_submissions_submitted_by';
        CREATE INDEX idx_gate_submissions_submitted_by
        ON governance.gate_submissions ("submitted_by");
    ELSE
        RAISE NOTICE 'Index idx_gate_submissions_submitted_by already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'graph_subscriptions'
        AND indexname = 'ux_graph_subscriptions_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_graph_subscriptions_record_locator';
        CREATE UNIQUE INDEX ux_graph_subscriptions_record_locator
        ON governance.graph_subscriptions ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_graph_subscriptions_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'graph_subscriptions'
        AND indexname = 'idx_graph_subscriptions_resource'
    ) THEN
        RAISE NOTICE 'Creating index idx_graph_subscriptions_resource';
        CREATE INDEX idx_graph_subscriptions_resource
        ON governance.graph_subscriptions ("resource");
    ELSE
        RAISE NOTICE 'Index idx_graph_subscriptions_resource already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'knowledge_chunks'
        AND indexname = 'ux_knowledge_chunks_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_knowledge_chunks_record_locator';
        CREATE UNIQUE INDEX ux_knowledge_chunks_record_locator
        ON governance.knowledge_chunks ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_knowledge_chunks_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'knowledge_chunks'
        AND indexname = 'idx_knowledge_chunks_document_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_knowledge_chunks_document_id';
        CREATE INDEX idx_knowledge_chunks_document_id
        ON governance.knowledge_chunks ("document_id");
    ELSE
        RAISE NOTICE 'Index idx_knowledge_chunks_document_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'knowledge_documents'
        AND indexname = 'ux_knowledge_documents_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_knowledge_documents_record_locator';
        CREATE UNIQUE INDEX ux_knowledge_documents_record_locator
        ON governance.knowledge_documents ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_knowledge_documents_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'meetings'
        AND indexname = 'ux_meetings_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_meetings_record_locator';
        CREATE UNIQUE INDEX ux_meetings_record_locator
        ON governance.meetings ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_meetings_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'meetings'
        AND indexname = 'idx_meetings_graph_online_meeting_id_status'
    ) THEN
        RAISE NOTICE 'Creating index idx_meetings_graph_online_meeting_id_status';
        CREATE INDEX idx_meetings_graph_online_meeting_id_status
        ON governance.meetings ("graph_online_meeting_id", "status");
    ELSE
        RAISE NOTICE 'Index idx_meetings_graph_online_meeting_id_status already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'notifications'
        AND indexname = 'ux_notifications_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_notifications_record_locator';
        CREATE UNIQUE INDEX ux_notifications_record_locator
        ON governance.notifications ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_notifications_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'notifications'
        AND indexname = 'idx_notifications_recipient_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_notifications_recipient_id';
        CREATE INDEX idx_notifications_recipient_id
        ON governance.notifications ("recipient_id");
    ELSE
        RAISE NOTICE 'Index idx_notifications_recipient_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'notifications'
        AND indexname = 'idx_notifications_recipient_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_notifications_recipient_id';
        CREATE INDEX idx_notifications_recipient_id
        ON governance.notifications ("recipient_id");
    ELSE
        RAISE NOTICE 'Index idx_notifications_recipient_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'notifications'
        AND indexname = 'idx_notifications_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_notifications_project_id';
        CREATE INDEX idx_notifications_project_id
        ON governance.notifications ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_notifications_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'projects'
        AND indexname = 'ux_projects_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_projects_record_locator';
        CREATE UNIQUE INDEX ux_projects_record_locator
        ON governance.projects ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_projects_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'projects'
        AND indexname = 'idx_projects_project_number_manager_id_status_created_at'
    ) THEN
        RAISE NOTICE 'Creating index idx_projects_project_number_manager_id_status_created_at';
        CREATE INDEX idx_projects_project_number_manager_id_status_created_at
        ON governance.projects ("project_number", "manager_id", "status", "created_at");
    ELSE
        RAISE NOTICE 'Index idx_projects_project_number_manager_id_status_created_at already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'projects'
        AND indexname = 'idx_projects_manager_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_projects_manager_id';
        CREATE INDEX idx_projects_manager_id
        ON governance.projects ("manager_id");
    ELSE
        RAISE NOTICE 'Index idx_projects_manager_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'projects'
        AND indexname = 'idx_projects_duplicate_of_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_projects_duplicate_of_id';
        CREATE INDEX idx_projects_duplicate_of_id
        ON governance.projects ("duplicate_of_id");
    ELSE
        RAISE NOTICE 'Index idx_projects_duplicate_of_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_approvals'
        AND indexname = 'ux_project_approvals_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_project_approvals_record_locator';
        CREATE UNIQUE INDEX ux_project_approvals_record_locator
        ON governance.project_approvals ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_project_approvals_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_approvals'
        AND indexname = 'idx_project_approvals_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_approvals_project_id';
        CREATE INDEX idx_project_approvals_project_id
        ON governance.project_approvals ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_project_approvals_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_approvals'
        AND indexname = 'idx_project_approvals_assigned_user_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_approvals_assigned_user_id';
        CREATE INDEX idx_project_approvals_assigned_user_id
        ON governance.project_approvals ("assigned_user_id");
    ELSE
        RAISE NOTICE 'Index idx_project_approvals_assigned_user_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_approvals'
        AND indexname = 'idx_project_approvals_approved_by'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_approvals_approved_by';
        CREATE INDEX idx_project_approvals_approved_by
        ON governance.project_approvals ("approved_by");
    ELSE
        RAISE NOTICE 'Index idx_project_approvals_approved_by already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_fields'
        AND indexname = 'ux_project_fields_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_project_fields_record_locator';
        CREATE UNIQUE INDEX ux_project_fields_record_locator
        ON governance.project_fields ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_project_fields_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_fields'
        AND indexname = 'idx_project_fields_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_fields_project_id';
        CREATE INDEX idx_project_fields_project_id
        ON governance.project_fields ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_project_fields_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_fields'
        AND indexname = 'idx_project_fields_updated_by_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_fields_updated_by_id';
        CREATE INDEX idx_project_fields_updated_by_id
        ON governance.project_fields ("updated_by_id");
    ELSE
        RAISE NOTICE 'Index idx_project_fields_updated_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_stakeholders'
        AND indexname = 'ux_project_stakeholders_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_project_stakeholders_record_locator';
        CREATE UNIQUE INDEX ux_project_stakeholders_record_locator
        ON governance.project_stakeholders ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_project_stakeholders_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_stakeholders'
        AND indexname = 'idx_project_stakeholders_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_stakeholders_project_id';
        CREATE INDEX idx_project_stakeholders_project_id
        ON governance.project_stakeholders ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_project_stakeholders_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'project_stakeholders'
        AND indexname = 'idx_project_stakeholders_user_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_project_stakeholders_user_id';
        CREATE INDEX idx_project_stakeholders_user_id
        ON governance.project_stakeholders ("user_id");
    ELSE
        RAISE NOTICE 'Index idx_project_stakeholders_user_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'risk_items'
        AND indexname = 'ux_risk_items_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_risk_items_record_locator';
        CREATE UNIQUE INDEX ux_risk_items_record_locator
        ON governance.risk_items ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_risk_items_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'risk_items'
        AND indexname = 'idx_risk_items_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_risk_items_project_id';
        CREATE INDEX idx_risk_items_project_id
        ON governance.risk_items ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_risk_items_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'risk_items'
        AND indexname = 'idx_risk_items_owner_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_risk_items_owner_id';
        CREATE INDEX idx_risk_items_owner_id
        ON governance.risk_items ("owner_id");
    ELSE
        RAISE NOTICE 'Index idx_risk_items_owner_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'task_assignments'
        AND indexname = 'ux_task_assignments_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_task_assignments_record_locator';
        CREATE UNIQUE INDEX ux_task_assignments_record_locator
        ON governance.task_assignments ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_task_assignments_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'task_assignments'
        AND indexname = 'idx_task_assignments_task_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_task_assignments_task_id';
        CREATE INDEX idx_task_assignments_task_id
        ON governance.task_assignments ("task_id");
    ELSE
        RAISE NOTICE 'Index idx_task_assignments_task_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'task_assignments'
        AND indexname = 'idx_task_assignments_assignee_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_task_assignments_assignee_id';
        CREATE INDEX idx_task_assignments_assignee_id
        ON governance.task_assignments ("assignee_id");
    ELSE
        RAISE NOTICE 'Index idx_task_assignments_assignee_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'task_assignments'
        AND indexname = 'idx_task_assignments_assigned_by_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_task_assignments_assigned_by_id';
        CREATE INDEX idx_task_assignments_assigned_by_id
        ON governance.task_assignments ("assigned_by_id");
    ELSE
        RAISE NOTICE 'Index idx_task_assignments_assigned_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'users'
        AND indexname = 'ux_users_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_users_record_locator';
        CREATE UNIQUE INDEX ux_users_record_locator
        ON governance.users ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_users_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'users'
        AND indexname = 'idx_users_email_username'
    ) THEN
        RAISE NOTICE 'Creating index idx_users_email_username';
        CREATE INDEX idx_users_email_username
        ON governance.users ("email", "username");
    ELSE
        RAISE NOTICE 'Index idx_users_email_username already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_definitions'
        AND indexname = 'ux_workflow_definitions_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_workflow_definitions_record_locator';
        CREATE UNIQUE INDEX ux_workflow_definitions_record_locator
        ON governance.workflow_definitions ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_workflow_definitions_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_instances'
        AND indexname = 'ux_workflow_instances_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_workflow_instances_record_locator';
        CREATE UNIQUE INDEX ux_workflow_instances_record_locator
        ON governance.workflow_instances ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_workflow_instances_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_instances'
        AND indexname = 'idx_workflow_instances_project_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_instances_project_id';
        CREATE INDEX idx_workflow_instances_project_id
        ON governance.workflow_instances ("project_id");
    ELSE
        RAISE NOTICE 'Index idx_workflow_instances_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_instances'
        AND indexname = 'idx_workflow_instances_definition_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_instances_definition_id';
        CREATE INDEX idx_workflow_instances_definition_id
        ON governance.workflow_instances ("definition_id");
    ELSE
        RAISE NOTICE 'Index idx_workflow_instances_definition_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_stages'
        AND indexname = 'ux_workflow_stages_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_workflow_stages_record_locator';
        CREATE UNIQUE INDEX ux_workflow_stages_record_locator
        ON governance.workflow_stages ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_workflow_stages_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_stages'
        AND indexname = 'idx_workflow_stages_status'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_stages_status';
        CREATE INDEX idx_workflow_stages_status
        ON governance.workflow_stages ("status");
    ELSE
        RAISE NOTICE 'Index idx_workflow_stages_status already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_stages'
        AND indexname = 'idx_workflow_stages_workflow_instance_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_stages_workflow_instance_id';
        CREATE INDEX idx_workflow_stages_workflow_instance_id
        ON governance.workflow_stages ("workflow_instance_id");
    ELSE
        RAISE NOTICE 'Index idx_workflow_stages_workflow_instance_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_stages'
        AND indexname = 'idx_workflow_stages_stage_definition_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_stages_stage_definition_id';
        CREATE INDEX idx_workflow_stages_stage_definition_id
        ON governance.workflow_stages ("stage_definition_id");
    ELSE
        RAISE NOTICE 'Index idx_workflow_stages_stage_definition_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_stage_definitions'
        AND indexname = 'ux_workflow_stage_definitions_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_workflow_stage_definitions_record_locator';
        CREATE UNIQUE INDEX ux_workflow_stage_definitions_record_locator
        ON governance.workflow_stage_definitions ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_workflow_stage_definitions_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_stage_definitions'
        AND indexname = 'idx_workflow_stage_definitions_workflow_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_stage_definitions_workflow_id';
        CREATE INDEX idx_workflow_stage_definitions_workflow_id
        ON governance.workflow_stage_definitions ("workflow_id");
    ELSE
        RAISE NOTICE 'Index idx_workflow_stage_definitions_workflow_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_tasks'
        AND indexname = 'ux_workflow_tasks_record_locator'
    ) THEN
        RAISE NOTICE 'Creating index ux_workflow_tasks_record_locator';
        CREATE UNIQUE INDEX ux_workflow_tasks_record_locator
        ON governance.workflow_tasks ("record_locator");
    ELSE
        RAISE NOTICE 'Index ux_workflow_tasks_record_locator already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM pg_indexes
        WHERE schemaname = 'governance'
        AND tablename = 'workflow_tasks'
        AND indexname = 'idx_workflow_tasks_stage_id'
    ) THEN
        RAISE NOTICE 'Creating index idx_workflow_tasks_stage_id';
        CREATE INDEX idx_workflow_tasks_stage_id
        ON governance.workflow_tasks ("stage_id");
    ELSE
        RAISE NOTICE 'Index idx_workflow_tasks_stage_id already exists';
    END IF;




    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'attachments'
        AND constraint_name = 'fk_attachments_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_attachments_project_id';
        ALTER TABLE governance.attachments
        ADD CONSTRAINT fk_attachments_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_attachments_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'attachments'
        AND constraint_name = 'fk_attachments_uploaded_by_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_attachments_uploaded_by_id';
        ALTER TABLE governance.attachments
        ADD CONSTRAINT fk_attachments_uploaded_by_id
        FOREIGN KEY ("uploaded_by_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_attachments_uploaded_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'audit_events'
        AND constraint_name = 'fk_audit_events_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_audit_events_project_id';
        ALTER TABLE governance.audit_events
        ADD CONSTRAINT fk_audit_events_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_audit_events_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'audit_events'
        AND constraint_name = 'fk_audit_events_performed_by_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_audit_events_performed_by_id';
        ALTER TABLE governance.audit_events
        ADD CONSTRAINT fk_audit_events_performed_by_id
        FOREIGN KEY ("performed_by_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_audit_events_performed_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'checklist_items'
        AND constraint_name = 'fk_checklist_items_task_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_checklist_items_task_id';
        ALTER TABLE governance.checklist_items
        ADD CONSTRAINT fk_checklist_items_task_id
        FOREIGN KEY ("task_id")
        REFERENCES governance.workflow_tasks("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_checklist_items_task_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'checklist_items'
        AND constraint_name = 'fk_checklist_items_completed_by_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_checklist_items_completed_by_id';
        ALTER TABLE governance.checklist_items
        ADD CONSTRAINT fk_checklist_items_completed_by_id
        FOREIGN KEY ("completed_by_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_checklist_items_completed_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'comments'
        AND constraint_name = 'fk_comments_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_comments_project_id';
        ALTER TABLE governance.comments
        ADD CONSTRAINT fk_comments_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_comments_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'comments'
        AND constraint_name = 'fk_comments_task_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_comments_task_id';
        ALTER TABLE governance.comments
        ADD CONSTRAINT fk_comments_task_id
        FOREIGN KEY ("task_id")
        REFERENCES governance.workflow_tasks("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_comments_task_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'comments'
        AND constraint_name = 'fk_comments_gate_review_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_comments_gate_review_id';
        ALTER TABLE governance.comments
        ADD CONSTRAINT fk_comments_gate_review_id
        FOREIGN KEY ("gate_review_id")
        REFERENCES governance.gate_reviews("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_comments_gate_review_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'comments'
        AND constraint_name = 'fk_comments_author_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_comments_author_id';
        ALTER TABLE governance.comments
        ADD CONSTRAINT fk_comments_author_id
        FOREIGN KEY ("author_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_comments_author_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'comments'
        AND constraint_name = 'fk_comments_parent_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_comments_parent_id';
        ALTER TABLE governance.comments
        ADD CONSTRAINT fk_comments_parent_id
        FOREIGN KEY ("parent_id")
        REFERENCES governance.comments("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_comments_parent_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'gate_reviews'
        AND constraint_name = 'fk_gate_reviews_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_gate_reviews_project_id';
        ALTER TABLE governance.gate_reviews
        ADD CONSTRAINT fk_gate_reviews_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_gate_reviews_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'gate_reviews'
        AND constraint_name = 'fk_gate_reviews_decision_by_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_gate_reviews_decision_by_id';
        ALTER TABLE governance.gate_reviews
        ADD CONSTRAINT fk_gate_reviews_decision_by_id
        FOREIGN KEY ("decision_by_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_gate_reviews_decision_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'gate_submissions'
        AND constraint_name = 'fk_gate_submissions_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_gate_submissions_project_id';
        ALTER TABLE governance.gate_submissions
        ADD CONSTRAINT fk_gate_submissions_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_gate_submissions_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'gate_submissions'
        AND constraint_name = 'fk_gate_submissions_submitted_by'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_gate_submissions_submitted_by';
        ALTER TABLE governance.gate_submissions
        ADD CONSTRAINT fk_gate_submissions_submitted_by
        FOREIGN KEY ("submitted_by")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_gate_submissions_submitted_by already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'knowledge_chunks'
        AND constraint_name = 'fk_knowledge_chunks_document_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_knowledge_chunks_document_id';
        ALTER TABLE governance.knowledge_chunks
        ADD CONSTRAINT fk_knowledge_chunks_document_id
        FOREIGN KEY ("document_id")
        REFERENCES governance.knowledge_documents("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_knowledge_chunks_document_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'notifications'
        AND constraint_name = 'fk_notifications_recipient_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_notifications_recipient_id';
        ALTER TABLE governance.notifications
        ADD CONSTRAINT fk_notifications_recipient_id
        FOREIGN KEY ("recipient_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_notifications_recipient_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'notifications'
        AND constraint_name = 'fk_notifications_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_notifications_project_id';
        ALTER TABLE governance.notifications
        ADD CONSTRAINT fk_notifications_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_notifications_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'projects'
        AND constraint_name = 'fk_projects_manager_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_projects_manager_id';
        ALTER TABLE governance.projects
        ADD CONSTRAINT fk_projects_manager_id
        FOREIGN KEY ("manager_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_projects_manager_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'projects'
        AND constraint_name = 'fk_projects_duplicate_of_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_projects_duplicate_of_id';
        ALTER TABLE governance.projects
        ADD CONSTRAINT fk_projects_duplicate_of_id
        FOREIGN KEY ("duplicate_of_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_projects_duplicate_of_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_approvals'
        AND constraint_name = 'fk_project_approvals_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_approvals_project_id';
        ALTER TABLE governance.project_approvals
        ADD CONSTRAINT fk_project_approvals_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_approvals_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_approvals'
        AND constraint_name = 'fk_project_approvals_assigned_user_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_approvals_assigned_user_id';
        ALTER TABLE governance.project_approvals
        ADD CONSTRAINT fk_project_approvals_assigned_user_id
        FOREIGN KEY ("assigned_user_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_approvals_assigned_user_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_approvals'
        AND constraint_name = 'fk_project_approvals_approved_by'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_approvals_approved_by';
        ALTER TABLE governance.project_approvals
        ADD CONSTRAINT fk_project_approvals_approved_by
        FOREIGN KEY ("approved_by")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_approvals_approved_by already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_fields'
        AND constraint_name = 'fk_project_fields_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_fields_project_id';
        ALTER TABLE governance.project_fields
        ADD CONSTRAINT fk_project_fields_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_fields_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_fields'
        AND constraint_name = 'fk_project_fields_updated_by_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_fields_updated_by_id';
        ALTER TABLE governance.project_fields
        ADD CONSTRAINT fk_project_fields_updated_by_id
        FOREIGN KEY ("updated_by_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_fields_updated_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_stakeholders'
        AND constraint_name = 'fk_project_stakeholders_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_stakeholders_project_id';
        ALTER TABLE governance.project_stakeholders
        ADD CONSTRAINT fk_project_stakeholders_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_stakeholders_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'project_stakeholders'
        AND constraint_name = 'fk_project_stakeholders_user_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_project_stakeholders_user_id';
        ALTER TABLE governance.project_stakeholders
        ADD CONSTRAINT fk_project_stakeholders_user_id
        FOREIGN KEY ("user_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_project_stakeholders_user_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'risk_items'
        AND constraint_name = 'fk_risk_items_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_risk_items_project_id';
        ALTER TABLE governance.risk_items
        ADD CONSTRAINT fk_risk_items_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_risk_items_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'risk_items'
        AND constraint_name = 'fk_risk_items_owner_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_risk_items_owner_id';
        ALTER TABLE governance.risk_items
        ADD CONSTRAINT fk_risk_items_owner_id
        FOREIGN KEY ("owner_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_risk_items_owner_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'task_assignments'
        AND constraint_name = 'fk_task_assignments_task_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_task_assignments_task_id';
        ALTER TABLE governance.task_assignments
        ADD CONSTRAINT fk_task_assignments_task_id
        FOREIGN KEY ("task_id")
        REFERENCES governance.workflow_tasks("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_task_assignments_task_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'task_assignments'
        AND constraint_name = 'fk_task_assignments_assignee_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_task_assignments_assignee_id';
        ALTER TABLE governance.task_assignments
        ADD CONSTRAINT fk_task_assignments_assignee_id
        FOREIGN KEY ("assignee_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_task_assignments_assignee_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'task_assignments'
        AND constraint_name = 'fk_task_assignments_assigned_by_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_task_assignments_assigned_by_id';
        ALTER TABLE governance.task_assignments
        ADD CONSTRAINT fk_task_assignments_assigned_by_id
        FOREIGN KEY ("assigned_by_id")
        REFERENCES governance.users("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_task_assignments_assigned_by_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'workflow_instances'
        AND constraint_name = 'fk_workflow_instances_project_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_workflow_instances_project_id';
        ALTER TABLE governance.workflow_instances
        ADD CONSTRAINT fk_workflow_instances_project_id
        FOREIGN KEY ("project_id")
        REFERENCES governance.projects("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_workflow_instances_project_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'workflow_instances'
        AND constraint_name = 'fk_workflow_instances_definition_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_workflow_instances_definition_id';
        ALTER TABLE governance.workflow_instances
        ADD CONSTRAINT fk_workflow_instances_definition_id
        FOREIGN KEY ("definition_id")
        REFERENCES governance.workflow_definitions("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_workflow_instances_definition_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'workflow_stages'
        AND constraint_name = 'fk_workflow_stages_workflow_instance_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_workflow_stages_workflow_instance_id';
        ALTER TABLE governance.workflow_stages
        ADD CONSTRAINT fk_workflow_stages_workflow_instance_id
        FOREIGN KEY ("workflow_instance_id")
        REFERENCES governance.workflow_instances("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_workflow_stages_workflow_instance_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'workflow_stages'
        AND constraint_name = 'fk_workflow_stages_stage_definition_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_workflow_stages_stage_definition_id';
        ALTER TABLE governance.workflow_stages
        ADD CONSTRAINT fk_workflow_stages_stage_definition_id
        FOREIGN KEY ("stage_definition_id")
        REFERENCES governance.workflow_stage_definitions("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_workflow_stages_stage_definition_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'workflow_stage_definitions'
        AND constraint_name = 'fk_workflow_stage_definitions_workflow_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_workflow_stage_definitions_workflow_id';
        ALTER TABLE governance.workflow_stage_definitions
        ADD CONSTRAINT fk_workflow_stage_definitions_workflow_id
        FOREIGN KEY ("workflow_id")
        REFERENCES governance.workflow_definitions("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_workflow_stage_definitions_workflow_id already exists';
    END IF;

    IF NOT EXISTS (
        SELECT 1
        FROM information_schema.table_constraints
        WHERE constraint_schema = 'governance'
        AND table_name = 'workflow_tasks'
        AND constraint_name = 'fk_workflow_tasks_stage_id'
        AND constraint_type = 'FOREIGN KEY'
    ) THEN
        RAISE NOTICE 'Creating foreign key constraint fk_workflow_tasks_stage_id';
        ALTER TABLE governance.workflow_tasks
        ADD CONSTRAINT fk_workflow_tasks_stage_id
        FOREIGN KEY ("stage_id")
        REFERENCES governance.workflow_stages("id")
        ON DELETE RESTRICT ON UPDATE CASCADE;
    ELSE
        RAISE NOTICE 'Foreign key constraint fk_workflow_tasks_stage_id already exists';
    END IF;








END $$;
