#![allow(dead_code)]

//
// Backend system: Generated Handler Defaults
//    Generated by app_gen.
//    Product-owned handler modules import these defaults and may override any
//    operation by defining a local function with the same name and signature.
//
pub(crate) mod schema {
    #[allow(unused_imports)]
    use std::sync::Arc;

    #[allow(unused_imports)]
    use crate::schemas::system::*;

    #[allow(unused_imports)]
    use crate::{
        product_api::{DataAccess, EntityType, HandlerResult, JsonValue, UserAuth},
        schemas::common::AggregateResult,
        schemas::system::{InputSchema, SchemaProjection, SchemaQueryResult},
    };
    pub(crate) async fn find_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
        id: String,
    ) -> HandlerResult<Option<SchemaProjection>> {
        let res = data_access
            .find_item::<SchemaProjection>(entity_type.clone(), selections, id, user)
            .await?;
        Ok(res)
    }

    pub(crate) async fn find_by_locator_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
        locator: String,
    ) -> HandlerResult<Option<SchemaProjection>> {
        let res = data_access
            .find_item_by_locator::<SchemaProjection>(
                entity_type.clone(),
                selections,
                locator,
                user,
            )
            .await?;
        Ok(res)
    }
    pub(crate) async fn get_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
    ) -> HandlerResult<Vec<SchemaProjection>> {
        let res = data_access
            .get_items::<SchemaProjection>(entity_type.clone(), selections, None, user)
            .await?;
        Ok(res)
    }
    pub(crate) async fn query_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
        filter: Option<JsonValue>,
        sort: Option<JsonValue>,
        skip: i32,
        limit: i32,
        after: Option<String>,
    ) -> HandlerResult<SchemaQueryResult> {
        let res = data_access
            .query_items::<SchemaProjection>(
                entity_type.clone(),
                selections,
                filter,
                sort,
                skip,
                limit,
                after,
                user,
            )
            .await?;
        let query_res = SchemaQueryResult {
            date_time: res.date_time,
            request_duration: res.request_duration,
            skip: res.skip,
            limit: res.limit,
            page_count: res.page_count,
            page_index: res.page_index,
            query_count: res.query_count,
            next_cursor: res.next_cursor,
            previous_cursor: res.previous_cursor,
            items: res.items,
        };
        Ok(query_res)
    }

    pub(crate) async fn aggregate_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        filter: Option<JsonValue>,
        group_by: Option<JsonValue>,
        metrics: Option<JsonValue>,
        having: Option<JsonValue>,
        sort: Option<JsonValue>,
        skip: i32,
        limit: i32,
    ) -> HandlerResult<AggregateResult> {
        let res = data_access
            .aggregate_items(
                entity_type.clone(),
                filter,
                group_by,
                metrics,
                having,
                sort,
                skip,
                limit,
                user,
            )
            .await?;
        Ok(res)
    }
    pub(crate) async fn create_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
        input: InputSchema,
    ) -> HandlerResult<SchemaProjection> {
        let res = data_access
            .create_item::<InputSchema, SchemaProjection>(
                entity_type.clone(),
                selections,
                input,
                user,
            )
            .await?;
        Ok(res)
    }
    pub(crate) async fn update_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
        input: InputSchema,
    ) -> HandlerResult<SchemaProjection> {
        let res = data_access
            .update_item::<InputSchema, SchemaProjection>(
                entity_type.clone(),
                selections,
                input,
                user,
            )
            .await?;
        Ok(res)
    }
    pub(crate) async fn delete_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        input: InputSchema,
    ) -> HandlerResult<i64> {
        let res = data_access
            .delete_item::<InputSchema>(entity_type.clone(), input, user)
            .await?;
        Ok(res)
    }
}
pub(crate) mod entity_type {
    #[allow(unused_imports)]
    use std::sync::Arc;

    #[allow(unused_imports)]
    use crate::schemas::system::*;

    #[allow(unused_imports)]
    use crate::{
        product_api::{DataAccess, EntityType, HandlerResult, JsonValue, UserAuth},
        schemas::common::AggregateResult,
        schemas::system::{EntityTypeProjection, InputEntityType},
    };
    #[allow(unused)]
    pub(crate) async fn get_schema_types_impl(
        user: Option<UserAuth>,
        data_access: &Arc<DataAccess>,
        entity_type: &Arc<EntityType>,
        selections: JsonValue,
        schema_name: String,
    ) -> HandlerResult<Vec<EntityType>> {
        Err(anyhow::anyhow!(
            "custom method `get_schema_types` is not implemented yet"
        ))
    }
}
