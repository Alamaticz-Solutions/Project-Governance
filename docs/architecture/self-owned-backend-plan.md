# Replacing the App Framework backend: scoping plan

**Status:** draft, no code written against this plan yet.
**Companion to:** the frontend replacement already shipped (`frontend/src/ui/kit.tsx` — see commit `f4d741d`, "Replace vendored @appfw/pds-health-components with a self-owned UI kit").

## Why this exists

The client's App Framework was supplied as a reference for how to structure Project Governance, not as something the shipped product should carry a runtime dependency on — especially since this product may be pitched to other clients. The frontend dependency (a vendored component library) was bounded and has been replaced already. The backend dependency is not bounded the same way: it is a code generator plus a runtime, and replacing it means replacing a small framework, not a component set. This document scopes that work before any of it starts.

## What the product actually depends on today

Five path-dependency crates, all resolved from a sibling `../../app-framework/` checkout (client IP, not present in this repo):

| Crate | What it does | Roughly how much of it the product exercises |
|---|---|---|
| `appfw_runtime` | Codegen input (`.appfw/model/**`) → GraphQL schema (async-graphql), request pipeline, JWT extraction (Okta + local-dev bypass), the `DataAccess` trait or provider-backed CRUD every service call goes through, Rego policy evaluation wiring, OpenTelemetry setup, config loading. Also ships `app_gen`, the CLI/codegen binary invoked by `scripts/appfw`. | Nearly all of it — this is the framework's core. |
| `appfw_provider_postgres` | Turns `RuntimeDataType` + a `serde_json::Value` into parameterized SQL for Postgres (insert/update/select), via `tokio-postgres`/`deadpool-postgres`. | All of it — this product is Postgres-only. |
| `appfw_mssql_auth` | MSSQL auth token acquisition for a provider we don't use (product is Postgres-only). | Effectively unused — dead weight. |
| `appfw_saas_core` | OAuth M2M token cache, redaction helpers, retry-after parsing — used by `backend/src/services/graph` (Microsoft Graph integration). | A handful of small, generic utility functions. |
| `appfw_test` | Test harness helpers for `api_tests`/`rego_test`. | A thin assertion/fixture layer. |

Two things already **aren't** framework-dependent and stay as-is regardless of what we do below:
- **Rego policy evaluation itself** — `regorus = "0.2"` (open-source Rust Rego engine) is already a direct `backend/Cargo.toml` dependency, separate from `appfw_runtime`. The `.rego` policy files under `.appfw/model/schemas/**/rbac/*.rego` and their generated wrappers under `backend/config/generated/schemas/**/*.rego` are product-owned, hand-authored content — only the *generator that wraps and publishes them* is framework code.
- **The database driver** — `tokio-postgres`/`deadpool-postgres`/`postgres-types` are already direct, open-source dependencies. `appfw_provider_postgres` sits on top of them; it doesn't replace them.

## Architecture options

**Option A — Hand-written, no generator.** Drop `app_gen` entirely. Write the GraphQL schema, resolvers, and Rego-check call sites directly in Rust for each of the 41 entities, by hand or via a one-time script that reads today's *generated* output and turns it into a static starting point. Ongoing schema changes become manual Rust edits instead of a model-config regeneration.
 - Pro: least new infrastructure to build; every line is inspectable, ordinary Rust.
 - Con: loses the model-as-source-of-truth workflow this session's audit relied on (`product validate`, `generate --check`, `policy-test`) — 41 entities × CRUD × Rego wiring by hand is a lot of repetitive code to keep in sync by discipline rather than by generation.

**Option B — Small self-owned generator, same shape as today.** Keep `.appfw/model/**` as the source of truth; write a much smaller `build.rs`/CLI (product-owned, in this repo) that reads it and emits the GraphQL schema + Rego wrappers + SQL DDL, dropping the parts of the real `app_gen` this product doesn't use (MSSQL/Snowflake/Mongo output, the frontend-contract generator now that the frontend owns its own UI — `src/generated/appfw-ui-contract.ts` would need a decision too, see Open questions).
 - Pro: keeps the workflow (`validate`/`generate --check`/`policy-test`) and the model-driven discipline this session depended on to fix findings A–Q safely.
 - Con: real engineering — a code generator is qualitatively harder to get right than the entities it generates; the deny-by-default Rego semantics (finding Q) is exactly the kind of subtle behavior a hand-rolled reimplementation could silently drop.

**Recommendation: Option B, scoped down hard.** Single provider (Postgres), single schema namespace (governance), no MSSQL/Snowflake/Mongo output, no `appfw_saas_core`/`appfw_mssql_auth` equivalent (fold their handful of functions directly into `backend/src/services/graph` and drop MSSQL auth entirely — it's unused). That cuts real scope, not just crate count.

## Proposed phases (in dependency order — each phase removes one path dependency)

1. **`appfw_mssql_auth` — delete, don't replace.** Unused in this product. Drop the dependency and the `mcp`/`kafka`/`sync` cargo features that pull in framework surface we don't use either, if audit confirms they're dead. Near-zero effort, do this first as a quick win.
2. **`appfw_saas_core` — inline.** Three small, generic, already-understood functions (`redact_json_value`, `parse_retry_after_seconds`, an OAuth M2M token cache + request-plan struct). Port them into `backend/src/services/graph/` directly as product code. Low effort, low risk — these are utility functions, not framework machinery.
3. **`appfw_provider_postgres` — reimplement directly on `tokio-postgres`.** We already depend on the underlying driver; this phase writes the `RuntimeDataType → SqlParam` mapping and insert/update/select statement builders as product code. This is also where finding N (the null-jsonb binding bug) lives — reimplementing it is a chance to fix it for real instead of carrying a session-local patch note. Medium effort — bounded (one file's worth of logic, `param.rs`/`mutation.rs` equivalents), well-understood after this session's audit.
4. **`appfw_runtime`'s non-generator parts — reimplement directly on `axum`/`async-graphql`/`jsonwebtoken`/`opentelemetry`.** JWT extraction + the local-dev bypass, the request pipeline, tracing setup, config loading. All standard patterns on top of libraries already in `backend/Cargo.toml`. Medium effort.
5. **`appfw_runtime`'s `DataAccess`/policy-check plumbing.** The piece that makes every `create_item`/`update_item` call run through Rego regardless of caller (the architectural fact behind finding Q) needs to be preserved exactly, not just approximately. **Done (2026-09-06).** Split into five sub-slices (pagination/provider-plan types, `ProviderIdentity`/`ProviderClient`, keyset cursor pagination, read-path orchestration, `AuditEvent`/`AuditQuery` hash chain + mutation-path orchestration), each committed and verified independently; last commit `87d1f37`. Note on verification: the `policy-test` suite mentioned above turned out to only assert a naming convention (`product_policy_contract_is_named_for_schema`) and doesn't exercise this plumbing at all — the thing that actually verified this phase was a live end-to-end check against the running server and Postgres: a `createComment` mutation produced a real `comments_audit` row with a computed `event_hash` and empty `prev_hash`, and a subsequent `updateComment` produced a second row whose `prev_hash` exactly matched the first row's `event_hash`, confirming the hash chain links correctly in production. Deliberately left in place, not part of this phase's core guarantee: the audit-timeline *read* path (`execute_audit_events_read`/`provider_query_audit_events`), `DataAccess::provider_descriptor`/`health_check`/`pool_stats`, the admin `diagnose_query` path, and `DatabaseClientRuntimeAdapter` itself (the shim satisfying `appfw_runtime::RuntimeProviderDataClient`, kept until the above are addressed in a follow-up pass).
6. **`app_gen` itself — the actual code generator.** Highest effort by far: parsing `.appfw/model/**`, rendering GraphQL schema + Rego wrappers + SQL DDL from it, reproducing today's `product validate`/`generate --check`/`boundary-check`/`feature-check`/`policy-test` CLI surface (`scripts/appfw`). This is the phase that turns "a product that happens to use a framework" back into "a product with its own model-driven backend" rather than one frozen at today's generated output. Do this last, once phases 1–5 have already proven the underlying pieces work without the framework. **Architecture scoping started (2026-09-06)** — see `docs/architecture/phase6-app-gen-scoping.md` for the traced module map, the actual (narrower-than-assumed) CLI surface this product depends on, a finding that the generated Rust output itself is still typed against `appfw_runtime` (a second migration bundled inside this phase, not just template porting), and a proposed 7-slice breakdown. No generator code has been written yet.

## Effort shape (rough, not a commitment)

Phases 1–3: small, days each. Phase 4: a couple of weeks. Phase 5: a couple of weeks, most of it verification rather than writing code. Phase 6: the generator — realistically the largest single piece of this whole plan, likely comparable in size to everything else combined. Worth treating as its own project once we get there, not a line item.

## Open questions to resolve before phase 6 starts

- **`src/generated/appfw-ui-contract.ts`** (frontend): currently `app_gen`-produced, describes the entity/relationship contract the frontend reads. If we own the generator, this is one more thing it emits — no new problem. If we ever stop generating entirely (Option A), the frontend needs a different source for this contract.
- **How much of the 41-entity, 38-relationship model is actually load-bearing** vs. legacy from the product-intake scaffold — worth an inventory pass before phase 6, since every entity the generator has to support is scope. **Resolved (2026-09-06): all of it — nothing to trim.** The base model is 30 entity-type files (24 in `governance`, 6 in `system`, one `_res.yaml` metadata file per schema excluded from the count) and 39 relationships across 4 files (`01-identity-project.yaml`: 6, `02-workflow.yaml`: 18, `03-supporting.yaml`: 14, `system/core.yaml`: 1) — close enough to the "41/38" figure that the gap is just imprecise counting, not missing entities. Separately, the running server logs `entity_types=64` at startup; that's not a discrepancy needing explanation away — it's the 30 base entity types plus their generated `_audit` companion tables (an `audit_entity_type` facet most entities apply) plus a handful of enum/computed pseudo-entity-types the registry also counts. Confirmed every one of the 24 governance entities is referenced by hand-written code outside `**/generated/**` (grep counts ranged 4–62 hits; `user` and `project` are the most central at 62 and 50). Postgres row counts were *not* a useful signal here — the product is pre-launch, so every table but the four touched by this session's own manual smoke tests (`comments`, `comments_audit`, `projects`, `projects_audit`) has zero rows; that reflects "no traffic yet," not "unused entity." **Conclusion for phase 6 scoping: the generator must support the full 30-entity-type/39-relationship model as-is — there is no scaffold to drop, so this doesn't shrink phase 6's scope, but it does remove the risk of the generator silently missing something during the port.**
- **Where the private registry fits in going forward.** Even after every phase above, `.cargo/config.toml`'s `pds-app-framework-crates` registry becomes moot for this product specifically — but if other teams/products still depend on it, that's a separate, client-side conversation, not something this plan needs to resolve.

## What this plan does *not* cover

It does not touch the frontend (already done) or propose changing product behavior — the goal throughout is to reproduce current behavior on self-owned code, verified against the same gates this session already used (`product validate`, `generate --check`, `policy-test`, live GraphQL testing), not to redesign the product while also swapping its foundation.

## Phase 7: remove `appfw_runtime` from the backend runtime (2026-09-07, in progress)

Phase 6 (above) replaced `app_gen`, the framework's *generator*. It did not touch the framework's *runtime* — `backend/Cargo.toml` still has one live path dependency left:

```
appfw_runtime = { package = "appfw-runtime", path = "../../app-framework/appfw_runtime", default-features = false }
```

(`appfw_saas_core`, `appfw_provider_postgres`, and `appfw_mssql_auth` are already gone from `backend/Cargo.toml` — phases 1–3 above are in fact done, just never marked so explicitly. Phase 4 ["appfw_runtime's non-generator parts"] and the deferred remainder of phase 5 are what's actually left, and this section supersedes both with a single concrete slice plan.)

**Scale, measured directly, not assumed -- and re-measured after the mcp/kafka/sync deletion below, excluding doc-comment mentions:** a naive `grep -rl appfw_runtime backend/src` counts 52 files, but several of those (`platform/{identifier,secrets,cors,host,security}.rs`) only mention `appfw_runtime` in a provenance doc comment ("ported off `appfw_runtime::identifier`") -- they're already fully self-owned from phases 4-5 and carry zero live dependency. Filtering comment-only lines out (`grep -v` on lines starting `//`/`//!`) gives the real count: **45 files, 295 code references**, concentrated hard at the top: `data/read_orchestration.rs` (59), `product_api.rs` (46), `data/mutation_orchestration.rs` (39), `data/data_access.rs` (36), `data/provider_identity.rs` (25), `data/query_ir_validation.rs` (12), `data/clients/database_client.rs` (9), `platform/policy.rs` (7) -- those 8 files alone are 233 of the 295 references (79%). The other 37 files share the remaining 62, almost all single- or double-line leaf-type imports (`RuntimeDataType`, `UserAuth`, `SecurityConfig`, and similar). The actual symbol surface used (`grep -rhoE "appfw_runtime::[A-Za-z0-9_]+(::[A-Za-z0-9_]+)*" backend/src | sort -u`) is bounded at roughly 30 distinct types/functions across error types, auth/JWT, query IR, the provider-operation dispatch enum, and platform plumbing -- enumerated in full in this repo's session history. The framework crate itself is 52,718 lines; the used surface is a small fraction of that.

**Why this is harder to verify than phases 5–6:** those were ports of *generators*, byte-diffable against real generated output. This is a port of a *runtime*, with no oracle. `cargo check -p backend` cannot run at all today because the sibling `../../app-framework` checkout was deleted per the client's IP-retention policy — meaning every one of the 56 files would be edited blind, with zero compiler feedback, until the very last reference is gone.

**Step zero (build aid, not a repo change):** the framework source is still readable from an earlier session's audit extraction. Its non-build-artifact contents were copied to `../app-framework` (i.e. `C:\Users\ManojRajakumar\Governance-Restructure\app-framework`, a sibling of `governance-appfw/`, **outside this repo and never committed**) purely so `cargo check -p backend` produces real diagnostics during the port. `appfw_runtime`'s own manifest depends on `appfw-saas-core` via the private `pds-app-framework-crates` registry (confirmed unreachable from this machine — `curl` times out), but since that crate's source is *also* present as a sibling path in the same extraction, Cargo resolves it locally without ever touching the registry. This local copy is deleted again in slice 8, once `backend/Cargo.toml` no longer references the framework at all — the acceptance test for the whole phase is a green build with the copy removed.

**IP decision, made autonomously per standing instruction:** delete `mcp`, `kafka`, and `sync` support rather than port it. None of the three appear in `default = ["http", "provider-postgres"]`; `operations/generated.rs` (6,857 lines) is dead code under those features today; phase 5's own notes recorded 171 compile errors behind the `mcp` feature previously; and this plan's own §"Proposed phases" step 1 already flagged mcp/kafka/sync as "framework surface we don't use" pending confirmation. Confirmed unused now. This shrinks `feature-check`'s matrix from 32 combinations to 4 (`http` × `provider-postgres`) and removes `mcp/mod.rs`, `kafka.rs`, `sync_workers.rs`, `operations/generated.rs` outright.

**Slices, in dependency order, each ending in a green `cargo check -p backend --all-targets` against the local framework copy:**

1. **Facade consolidation + dead-feature deletion.** Re-export every `appfw_runtime::*` symbol `backend/src` actually uses through `platform::runtime` (new module), then repoint all 56 files' imports at it. Delete mcp/kafka/sync per the decision above. Purely mechanical, no semantic change — the facade still re-exports the *framework's* types underneath. Result: `grep -rl appfw_runtime backend/src` returns one file (`platform/runtime.rs`).
2. **Leaf types.** `RuntimeError`, `MetadataError`, `ConfigError`, `DataStoreError`, `RuntimeDataType`/`model_metadata`, `json` helpers, `identifier`, `record_locator`. No dependency on anything else in the list.
3. **Auth/security.** `RuntimeJwtExtractor`, `RuntimeAuthState`, `SecurityConfig`, `UserAuth`, `RuntimePrincipalType`, `PolicyAccess` (align with `product_gen::policy`, already self-owned). Also finally resolves the `UserAuth::from` bridge phase 5 deferred.
4. **Query IR.** `RuntimeFilterOp`, `RuntimePagination`/`RuntimePaginationStrategy`, `query_filter`, `QueryCost`/`QueryCostBudget`.
5. **Provider contract + orchestration — the largest slice.** `RuntimeProviderOperation` (and its 10 variants), `RuntimeProviderPlanInput`, `RuntimeProviderOperationCounts`, `RuntimeJsonQueryResult`/`RuntimeJsonAggregateResult`, `ProviderPoolStats`, `provider_keys::FrameworkProvider`, `RuntimeHandlerContext`, `RuntimeAuditEvent`/`RuntimeAuditQuery`, and the `DatabaseClientRuntimeAdapter` shim phase 5 deliberately left in place. This is where `read_orchestration.rs`/`product_api.rs`/`mutation_orchestration.rs`/`data_access.rs`/`provider_identity.rs` live — expect it to be bigger than every other slice combined.
6. **Platform plumbing.** `cors`, `observability::RequestContext`, `host`, `secrets`, `tenant_isolation`, `routing::runtime_graphql_schema_routes`, `admin`, `product_ui::product_ui_routes_if_present`.
7. **`api_tests` off `appfw_test`.** Same move already made for `rego_test` (→ `product_gen::policy`) — either replace or delete the dependency, then restore `api_tests` to the root workspace `members` list.
8. **Final cutover.** Delete the `appfw_runtime` line from `backend/Cargo.toml`, delete the `.cargo/config.toml` `pds-app-framework-crates` registry stanza, delete the local `../app-framework` copy, and confirm `cargo check -p backend --all-targets` + `feature-check` (now 4 combinations) both stay green with **no framework present on disk anywhere**. That last condition is the actual acceptance test — a green build against the local copy proves nothing about independence by itself.

**On verification, stated plainly:** there is no byte-diff oracle for a runtime port. What actually verifies each slice: the compiler (exhaustive matches, real trait bounds — not a rubber stamp, given `RuntimeProviderOperation` alone has 10 variants a `match` must cover), the framework's own unit tests for each ported module (portable near-verbatim, same discipline used for `product_gen::policy`/`relationships`), and this product's own gates staying green throughout (`boundary-check`, `validate`, `generate --check`, `rego_test`). Live end-to-end GraphQL testing against real Postgres (the same method that verified phase 5's audit hash-chain) is used wherever a slice touches a request path, not just a type definition.
